interface:
  display_name: "MCU Configuration"
  short_description: "Configure MCU and IOC; guide CubeMX firmware setup"
  default_prompt: "Use $mcu-config to read or update workspace settings, select an IOC, and identify any CubeMX firmware setup needed in the sidebar."
