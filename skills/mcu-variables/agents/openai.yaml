interface:
  display_name: "MCU Variables"
  short_description: "Read variables, control sampling, or safely tune RAM"
  default_prompt: "Use $mcu-variables to inspect live MCU variables or control shared sampling as requested."
