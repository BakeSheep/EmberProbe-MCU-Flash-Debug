interface:
  display_name: "MCU Debug Control"
  short_description: "Control debugger sessions and breakpoints"
  default_prompt: "Use $mcu-debug-control to inspect or control the current debugger session and manage the requested breakpoints."
